# 4 복제 — 실서식 충실 복제 (학습 미포함 평가 앵커)
- `html/` : 원본 판독 → HTML 전사 100쪽. 규약은 `html/CONVENTIONS.md`, 공용 CSS `html/hwp.css`
- `render/` : `render_html.py` 산출 — PNG · `_gt.json` · `_labeled.png`, 대조 갤러리 `index.html`(원본|복제|GT)
- 역할 3가지: 부품·문법 채굴 소스 / 생성물↔실물 대조 기준 / 홀드아웃 평가
- 잔여 105쪽(표준·밀집 28·초밀집 13) 확장 대기

## P6-1 앵커 확장 (44 → 100쪽)

2차 배치로 **1편 44쪽 + 3편 12쪽 = 56쪽**을 추가 전사했다. GT 합계 2,234칸.
`lint.py` 위반 0 (기존 `2편_서식22호_사유서-1` 의 L6 1건은 이전부터 있던 것으로 손대지 않음),
`8_train/label_stage1.py` 순서 불일치 0.

| 묶음 | 쪽 | GT | 비고 |
|---|---|---|---|
| 1편 서식7·9·11·13·18호 | 5 | 681 | 대장·명부형. 반복 행만 있는 격자 |
| 1편 서식16-2호 | 9 | 373 | 서비스 이용가정 정보지. 척도표·점선 구분줄·중첩표 |
| 1편 서식1호 | 4 | 219 | 별지 제1호서식. 외곽 세로선 없는 이중괘선 블록표, 4쪽은 GT 0 |
| 1편 서식23호 | 3 | 69 | 사업계획서. 글머리표+기록 자리 |
| 1편 서식24호 | 23 | 682 | 01~12 실적 통계표·대상자 명부 / 13·15 백지 / 14 편 표지 / 16~23 변경 비교표(모두 GT 0) |
| 3편 서식1-1·1-2·1-3·10·10-1·12·13·14·15·16호 | 12 | 210 | 2편 대응 서식이 있는 것은 그 복제본에서 파생 후 원본 대조 |

어려웠던 패턴과 그 처리는 `NEW_PATTERNS.md` 에 따로 적었다. 요약:
대각선 분할 머리칸(SVG), 외곽 세로선 없는 이중괘선 블록표(테이블 분할 + `border-top:double`),
칸 안 인쇄 작성례(필드 밖 형제 span + 빈 cgf), 단독 글머리표 ○/□(CSS `::before` 로 그려 L6 회피),
번호 글머리 뒤 빈칸(절대배치 필드 겹치기), 서식이 아닌 책 앞붙이 9쪽의 음성 앵커 취급.

재생성:
```
./venv/bin/python 4_replica/render_html.py 4_replica/html/*.html --out 4_replica/render
./venv/bin/python 4_replica/lint.py
```
