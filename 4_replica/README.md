# 4 복제 — 실서식 충실 복제 (학습 미포함 평가 앵커)
- `html/` : 원본 판독 → HTML 전사 44쪽(1차). 규약은 `html/CONVENTIONS.md`, 공용 CSS `html/hwp.css`
- `render/` : `render_html.py` 산출 — PNG · `_gt.json` · `_labeled.png`, 대조 갤러리 `index.html`(원본|복제|GT)
- 역할 3가지: 부품·문법 채굴 소스 / 생성물↔실물 대조 기준 / 홀드아웃 평가
- 잔여 105쪽(표준·밀집 28·초밀집 13) 확장 대기

## 전사 이력 — P6-1 앵커 확장(2편 42쪽 + 3편 뒤쪽 15쪽 = 57쪽)
- 2편 전 서식(1·2·3·4-1·5·6·7·8·9·11·12·19·20·23·24·25·26·27·28·29호) 42쪽, 3편은 정렬 13번째부터 끝까지
  (1-4·4·6·8·20·24·25호, 서식1호 3쪽 포함) 15쪽. 합계 **57쪽 · GT 3,738개 · 쪽당 중앙값 29**.
  `lint.py` 위반 0(경고 2건은 원본에도 비어 있는 칸). `render/` 에 PNG·`_gt.json`·`_labeled.png` 동봉.
- **2편/3편 서식1호**(사회보장급여 신청(변경)서 3쪽)는 본문 영역 픽셀 diff 0 — 조판 공백 1곳만 달라
  같은 전사본을 두 파일로 두었다. 3편 1-4·20·24·8호도 2편 대응 서식에서 문구·열 차이만 반영해 파생.
- GT 0 음성 앵커 7쪽: 2편 27호-2(작성요령), 6호-2(약관), 2호-06·11(안내문), 5호-1·3편 4호-1(안내문 1필드),
  3편 25호-2(백지)·25호-3(판권지).
- 어려웠던 패턴(자세한 처리는 `NEW_PATTERNS.md`)
  - **초밀집 격자**: 2편 11호(9일자×37행, 360필드), 25호(25열×14행, 350), 24호(176), 26호(168), 29호(170),
    3편 4호-2(5개 기관 블록, 215). 행 높이 18~27px 로 줄이고 `.cgf` 인셋을 2px 로 좁혔다.
  - **한글이 섞인 작성례(prefill)**: 규약대로 data-f 안에 넣으면 lint L2 에 걸려, 인쇄 글자는 td 에 두고
    같은 칸을 빈 `span.cgf` 로 덮었다(칸 전체가 GT).
  - **글머리 ○ 뒤가 빈 줄**: L6 오탐을 피해 `::before` 로 그렸다.
  - **O표로 고르는 인쇄 낱말**(2편·3편 6호): WORD 규약(4자 이하)을 넘는 항목이 섞여 전부 인쇄 텍스트로 두었다.
  - **2편 2호 15쪽**: 급여 종류별 결정문이 쪽마다 다른 표로 반복되는 초밀집 통지서. 12쪽은 [  ] 마커 68개,
    14쪽 처리절차는 inline SVG 로 그렸다.
- `render/index.html` 갤러리는 다른 배치와의 충돌을 피해 이번 배치에서 갱신하지 않았다(재생성 필요).
